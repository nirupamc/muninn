import { useHealth } from "../../hooks/useHealth";
export function TopBar() { const { data, loading, error } = useHealth(); const online = !!data && data.status === "ok" && !error; return <header className="operations-topbar"><div className="topbar-wordmark"><strong>MUNIN</strong><span>MEMORY OPERATIONS // NERV-01</span></div><div className="topbar-rule" /><div className="topbar-status"><span>ENGINE</span><b className={online ? "nominal" : "warning"}>{online ? "NOMINAL" : loading ? "CHECKING" : "OFFLINE"}</b></div></header>; }
