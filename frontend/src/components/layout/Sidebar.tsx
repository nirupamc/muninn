import { useState } from "react";
import { NavLink } from "react-router-dom";
import { useScope } from "../../lib/scope";
const PRIMARY = [["01", "/overview", "OVERVIEW", "CORE STATUS"], ["02", "/graph", "MEMORY", "NETWORK"], ["03", "/memories", "MEMORY", "INDEX"], ["04", "/context", "CONTEXT", "ASSEMBLY"], ["05", "/timeline", "TEMPORAL", "TRACE"], ["06", "/conflicts", "CONFLICT", "CENTER"]] as const;
const SECONDARY = [["/projects", "PROJECTS"], ["/agents", "AGENTS"], ["/status", "STATUS"]] as const;
export function Sidebar() { const { namespace, setNamespace } = useScope(); const [open, setOpen] = useState(false); return <><button className="mobile-nav-trigger" type="button" onClick={() => setOpen(true)} aria-label="Open operations navigation">MUNIN // SECTORS</button>{open && <button className="mobile-nav-scrim" type="button" aria-label="Close navigation" onClick={() => setOpen(false)} />}<nav className={`operations-sidebar ${open ? "mobile-open" : ""}`} aria-label="Operational sectors"><div className="sidebar-identity"><button type="button" onClick={() => setOpen(false)} className="sidebar-close" aria-label="Close navigation">×</button><span>MEMORY OPERATIONS</span><strong>MUNIN</strong><small>NERV-01 // ACTIVE</small></div><div className="sector-list">{PRIMARY.map(([number, to, line1, line2]) => <NavLink key={to} to={to} onClick={() => setOpen(false)} className={({ isActive }) => `sector-link ${isActive ? "active" : ""}`}><span className="sector-number">{number}</span><span>{line1}<b>{line2}</b></span><i aria-hidden="true" /></NavLink>)}</div><div className="scope-control"><label htmlFor="active-scope">CURRENT OPERATING SCOPE</label><input id="active-scope" className="munin-input" value={namespace} spellCheck={false} onChange={(event) => setNamespace(event.target.value.trim() || "project:munin")} /></div><div className="secondary-nav">{SECONDARY.map(([to, label]) => <NavLink key={to} to={to} onClick={() => setOpen(false)} className={({ isActive }) => isActive ? "active" : ""}>{label}</NavLink>)}</div><div className="lifecycle-rail"><span>MEMORY LIFECYCLE</span><i className="active" /><i className="superseded" /><i className="invalidated" /><i className="archived" /></div></nav></>; }
